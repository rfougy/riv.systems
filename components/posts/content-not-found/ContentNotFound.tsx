import React from "react";
import NotFound from "../../shared/not-found/NotFound";

const ContentNotFound: React.FC = () => (
  <NotFound
    title="NO CONTENT"
    copy="The content you're searching for cannot be found."
  />
);

export default ContentNotFound;
