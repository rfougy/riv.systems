const WorksGrid: React.FC<{
  link: string;
  roles: string[];
  duration: string[];
  tools: string[];
}> = ({ link, roles, duration, tools }) => {
  return <div>WorksGrid</div>;
};

export default WorksGrid;
