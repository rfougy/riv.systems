export interface IAudioControlsButtonDict {
  play: {
    icon: any;
    ariaLabel: string;
    alt: string;
  };
  pause: {
    icon: any;
    ariaLabel: string;
    alt: string;
  };
  nextSkip: {
    icon: any;
    ariaLabel: string;
    alt: string;
  };
  prevSkip: {
    icon: any;
    ariaLabel: string;
    alt: string;
  };
}
