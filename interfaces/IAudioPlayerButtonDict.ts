export interface IAudioPlayerButtonDict {
  play: {
    icon: any;
    ariaLabel: string;
    imageAlt: string;
  };
  pause: {
    icon: any;
    ariaLabel: string;
    imageAlt: string;
  };
  nextSkip: {
    icon: any;
    ariaLabel: string;
    imageAlt: string;
  };
  prevSkip: {
    icon: any;
    ariaLabel: string;
    imageAlt: string;
  };
}
